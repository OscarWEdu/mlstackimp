# How to Install and Run

- Install Node.js and .NET 10.
- Pull down and open the project in an editor of your choice.
- (You may need to run the command 'dotnet restore' if your backend dependencies are not up to date).
- Run npm install to install all frontend packages and dependencies.
- Launch the application with 'npm run dev'.
- Open the website at: http://localhost:5173